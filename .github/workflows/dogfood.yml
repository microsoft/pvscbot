name: "Dogfood"

on:
  issues:
    types: ["opened"]

jobs:
  pvscbot:
    runs-on: ubuntu-latest
    steps:
    - uses: microsoft/pvscbot@v1.0.0
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
