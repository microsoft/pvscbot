name: "Dogfood"

on:
  issues:
    types: ["opened"]

jobs:
  pvscbot:
    runs-on: ubuntu-latest
    steps:
    # docker://brettcannon/pvscbot:latest
    # microsoft/pvscbot@gh-action
    - uses: microsoft/pvscbot@gh-action
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
